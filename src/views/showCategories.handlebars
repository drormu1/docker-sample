   <h1>All Categories</h1>
    <a href="/add-category">Add New Category</a> <!-- Link to Add Category page -->
    <ul>
        {{#each categories}}
            <li>{{this.title}}</li>
        {{/each}}
    </ul>